image: electronuserland/electron-builder:wine

cache:
  paths:
    - node_modules/

before_script:
  - npm install -g n
  - n 10.16.3
  - npm install

stages:
  - deploy

publish:
  stage: deploy
  only:
    - tags
  script:
    - npm run build
    - curl --request POST --header "Private-Token:MAzPbYT-xgaUgx2Nc4di" --form "file=@/builds/Rett/coordinator/build/coordinator Setup $CI_COMMIT_REF_NAME.exe" "https://gitlab.com/api/v4/projects/18471086/uploads"
    - curl --request POST --header 'Content-Type:application/json' --header "Private-Token:MAzPbYT-xgaUgx2Nc4di" --data '{"name":"Version $CI_COMMIT_REF_NAME", "tag_name":"$CI_COMMIT_REF_NAME", "description":"https://gitlab.com/api/v4/projects/18471086/uploads/coordinator Setup $CI_COMMIT_REF_NAME.exe"}' "https://gitlab.com/api/v4/projects/18471086/releases"